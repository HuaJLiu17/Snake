//this file is used to define constants that can be accessed by any other server codebases

const FRAME_RATE = 10;
const GRID_SIZE = 20;

//modules are self-contained units of functionality that can be shared and reused across projects
module.exports = {
    FRAME_RATE,
    GRID_SIZE,
}

